<%= form_for([@post, @post.comments.new]) do |f| %>
  <%= f.label :title %><br>
  <%= f.text_field :title%><br>
  <br>
  <%= f.label :body %><br>
  <%= f.text_area :body %><br>
  <%= f.submit%>
<% end %>
